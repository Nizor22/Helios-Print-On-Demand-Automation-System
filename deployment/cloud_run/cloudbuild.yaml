steps:
  # Build and deploy orchestrator service only (for testing)
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - 'run'
    - 'deploy'
    - 'helios-orchestrator'
    - '--source'
    - '.'
    - '--region'
    - 'us-central1'
    - '--platform'
    - 'managed'
    - '--memory'
    - '512Mi'
    - '--allow-unauthenticated'
    - '--set-env-vars'
    - 'SERVICE_TYPE=orchestrator'

timeout: 1800s
options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY

